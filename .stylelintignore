# By default, Stylelint ignores the `node_modules` directory.
# See https://stylelint.io/user-guide/ignore-code and https://stylelint.io/user-guide/configure#ignorefiles for more details.

# Eleventy output directory
/public/
